#import math (abaixo exemplode importação apenas de uma funcção de uma biblioteca)
from math import sqrt
num= int(input('Digite um número: '))
raiz = sqrt(num)
print('A rais de {}, tem o resultado de {:.2f}!!'.format(num, raiz))
